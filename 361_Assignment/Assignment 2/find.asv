function Count = find(arr,threshold)
    arr = [arr arr];
    d1 = find(arr > threshold);
    d2 = find(arr < threshold);
    max =0;
    gapd1=0;
    gapd2=0;
    for i =1:min(size(d1),size(d2))
        gapd2 = d2(i)
    end
    Count = max;
end